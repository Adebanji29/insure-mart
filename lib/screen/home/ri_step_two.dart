import 'package:flutter/material.dart';

class RIStepTwo extends StatelessWidget {
  const RIStepTwo({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
